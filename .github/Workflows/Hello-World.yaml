name: Hello World 
on:
    push:
        branches:
            - main
    pull_requests:
        - main
    workflow_dispatch:
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v2
          - name: hello-world
            run: echo "Hello world"
            shell: bash
    goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: goodbye
              run: echo "GoodBye"
              shell: bash